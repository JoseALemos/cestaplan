{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "apps/api/Dockerfile"
  },
  "deploy": {
    "startCommand": "python -m cestaplan_api.jobs.purge_audit_logs --commit",
    "cronSchedule": "0 3 * * 1",
    "numReplicas": 1,
    "restartPolicyType": "NEVER"
  }
}
